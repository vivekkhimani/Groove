'''
A script for visualizing the data classified by the resultant clusters. We will examine the following methods:
	a) PCA (reduced dimensions)
	b) T-SNE
Once the visualization plots are obtained, we plan to display it on the web interface.
'''
import numpy as np
import pandas as pd

import matplotlib.pyplot as plt
from scipy.sparse import csr_matrix
from mpl_toolkits.axes_grid1 import make_axes_locatable

def visualize_clusters(data):
	return

#driver
if __name__ == '__main__':
	print("driver")